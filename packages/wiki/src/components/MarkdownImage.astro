---
const props = Astro.props;
const src = typeof props.src === "string" ? props.src : props.src?.src || "";
const alt = props.alt || "";
const { src: _src, alt: _alt, ...rest } = props;
---

<image-lightbox>
  <img src={src} alt={alt} {...rest} />
</image-lightbox>
